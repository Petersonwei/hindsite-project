<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hindsite" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <link rel="icon" href="/HINDSITE-NAV.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/HINDSITE-NAV.svg">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <style>
      .navbar-brand img {
        height: 40px;
        vertical-align: middle;
      }
    </style>
  </head>

  <body>
    <header class="bg-dark">
      <nav class="navbar container">
        <%= link_to root_path, class: "navbar-brand" do %>
          <img src="/HINDSITE-NAV-white.svg" alt="Hindsite Logo" class="navbar-brand-img" style="height: 40px;">
        <% end %>
        
        <button id="mobile-menu-toggle" class="mobile-menu-toggle">
          <i class="fas fa-bars"></i>
        </button>

        <div class="nav-links">
          <% if logged_in? %>
            <%= link_to "Posts", posts_path, class: "nav-link #{current_page?(posts_path) ? 'active' : ''}" %>
            <%= link_to "Organisations", organisations_path, class: "nav-link #{controller_name == 'organisations' ? 'active' : ''}" %>
            <%= link_to "Users", users_path, class: "nav-link #{controller_name == 'users' ? 'active' : ''}" %>
            <div class="ml-4 flex items-center gap-2">
              <span class="badge badge-outline text-light">
                <%= current_user.name %>
              </span>
              <%= link_to "Logout", logout_path, class: "btn btn-primary btn-sm", data: { turbo_method: :delete } %>
            </div>
          <% else %>
            <%= link_to "Login", login_path, class: "btn btn-outline text-light" %>
            <%= link_to "Sign Up", new_user_path, class: "btn btn-primary ml-2" %>
          <% end %>
        </div>
      </nav>
    </header>

    <div id="mobile-menu" class="mobile-menu">
      <div class="mobile-menu-header">
        <img src="/HINDSITE-NAV-white.svg" alt="Hindsite Logo" style="height: 30px;">
        <button id="mobile-menu-close" class="mobile-menu-close">
          <i class="fas fa-times"></i>
        </button>
      </div>
      <div class="mobile-nav-links">
        <% if logged_in? %>
          <%= link_to "Posts", posts_path, class: "mobile-nav-link" %>
          <%= link_to "Organisations", organisations_path, class: "mobile-nav-link" %>
          <%= link_to "Users", users_path, class: "mobile-nav-link" %>
          <div class="mt-4 flex flex-col gap-2">
            <span class="badge badge-outline text-light">
              <%= current_user.name %>
            </span>
            <%= link_to "Logout", logout_path, class: "btn btn-primary", data: { turbo_method: :delete } %>
          </div>
        <% else %>
          <%= link_to "Login", login_path, class: "mobile-nav-link" %>
          <%= link_to "Sign Up", new_user_path, class: "mobile-nav-link" %>
        <% end %>
      </div>
    </div>

    <% if flash.any? %>
      <div class="container mt-4">
        <% flash.each do |type, message| %>
          <div class="alert <%= type == 'alert' ? 'alert-error' : 'alert-success' %>">
            <%= message %>
          </div>
        <% end %>
      </div>
    <% end %>

    <% if !logged_in? && current_page?(root_path) %>
      <section class="hero">
        <img src="/mining truck.jpg" alt="Mining operations" class="hero-bg-image">
        <div class="container">
          <div class="hero-content">
            <h1 class="hero-title">Organisation Management System</h1>
            <p class="hero-description">A powerful platform to manage your organisation's workflows, posts, and team members with ease.</p>
            <div class="hero-actions">
              <%= link_to "Sign Up", new_user_path, class: "btn btn-primary btn-lg" %>
              <%= link_to "Login", login_path, class: "btn btn-outline text-light ml-4" %>
            </div>
          </div>
        </div>
      </section>
      
      <section class="section">
        <div class="container">
          <h2 class="section-title text-center">Features</h2>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-8">
            <div class="card text-center p-6">
              <i class="fas fa-users text-3xl text-primary mb-4"></i>
              <h3 class="text-xl font-semibold mb-2">Multi-Organisation Support</h3>
              <p>Connect with multiple organisations and manage different teams from a single account.</p>
            </div>
            <div class="card text-center p-6">
              <i class="fas fa-clipboard-list text-3xl text-primary mb-4"></i>
              <h3 class="text-xl font-semibold mb-2">Post Management</h3>
              <p>Create, share, and organize posts within your organisation for better team communication.</p>
            </div>
            <div class="card text-center p-6">
              <i class="fas fa-shield-alt text-3xl text-primary mb-4"></i>
              <h3 class="text-xl font-semibold mb-2">Secure & Reliable</h3>
              <p>Enterprise-grade security ensures your organisation's data remains protected at all times.</p>
            </div>
          </div>
        </div>
      </section>
    <% end %>

    <main>
      <div class="container">
        <%= yield %>
      </div>
    </main>

    <footer class="bg-dark">
      <div class="container">
        <div class="flex justify-between items-center">
          <div>
            <img src="/HINDSITE-NAV-white.svg" alt="Hindsite Logo" style="height: 30px; margin-bottom: 8px;">
            <p class="text-sm text-light">Organisation Management System</p>
          </div>
          <div>
            <p class="text-sm text-light">&copy; <%= Date.today.year %> Hindsite Inc. All rights reserved.</p>
          </div>
        </div>
      </div>
    </footer>

    <script>
      document.addEventListener('DOMContentLoaded', function() {
        const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
        const mobileMenuClose = document.getElementById('mobile-menu-close');
        const mobileMenu = document.getElementById('mobile-menu');
        
        if (mobileMenuToggle && mobileMenuClose && mobileMenu) {
          mobileMenuToggle.addEventListener('click', function() {
            mobileMenu.classList.add('active');
          });
          
          mobileMenuClose.addEventListener('click', function() {
            mobileMenu.classList.remove('active');
          });
        }
      });
    </script>
  </body>
</html>
